(function(App) {
    "use strict";

    var MovieList = Backbone.View.extend({
        render: function() {
        },

        load: function(filter) {

        }
    });

    App.View.MovieList = MovieList;
})(window.App);