(function(context) {
    "use strict";

    context.App = context.App || {};

    var Config = {
        title: 'Popcorn Time',
        platform: process.platform
    };

    context.App.Config = Config;
})(window);